[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "dtx"
version = "0.1.0"
description = "A minimalist dotfiles manager"
readme = "README.md"
requires-python = ">=3.8"

[project.scripts]
dtx = "dtx.cli:main"source .venv/bin/activate   